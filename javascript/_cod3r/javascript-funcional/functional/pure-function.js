// Pure Function
function sum(a, b) {
  return a + b
}

console.log(sum(7, 14))

// Pure Function
// Functions that don't depend on or interfere with any other data
// Always returns the same result if the same arguments are passed, without any observable side effects
