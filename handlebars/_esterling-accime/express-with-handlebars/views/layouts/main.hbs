<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{{ title }}</title>
  </head>
  <body>
    <div class="Breadcrumb">
      {{#each breadcrumb as | item key | }}
        {{#if key}} / {{/if}}
        {{ name }}
      {{/each}}
    </div>

    {{{ body }}}
  </body>
</html>
