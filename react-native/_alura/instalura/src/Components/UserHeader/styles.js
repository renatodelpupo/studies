import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
  userHeader: {
    alignItems: "center",
    flexDirection: "row"
  },

  userName: {
    fontWeight: '700'
  },

  userPhoto: {
    borderRadius: 30,
    height: 40,
    margin: 10,
    width: 40
  }
})

export default styles