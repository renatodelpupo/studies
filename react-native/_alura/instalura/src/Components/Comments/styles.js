import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
  addComment: {
    flex: 1
  },

  addCommentWrap: {
    alignItems: 'center',
    flexDirection: 'row',
    padding: (0, 10)
  },

  sendImage: {
    height: 30,
    width: 30
  }
})

export default styles